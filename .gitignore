/target
/graphify-out
# Tampon de sortie de simu/tests collé par l'utilisateur (voir flux ch.7) — jamais versionné.
result.md
# Notes PRIVÉES (infos sensibles : IP maison/mobile, serveur, repos ; récap de session ;
# chantier Unreal ; carnet « où trouver quoi ») → JAMAIS poussées. Index + notes dans prive/.
REPRISE-PRIVEE.md
/prive/
# Assets 3D (mesh/glb) : ils n'ont rien à faire dans le dépôt du cœur réseau — gardés en local.
/asset/
# Courbes exportées par le banc `jeu vivant` (CSV + SVG : artefacts locaux de visualisation, régénérables).
vivant_courbes*
# WAV écrits par `jeu separe wav` (audition des bruits isolés : artefacts locaux, régénérables).
/voix_wav/
# Spectrogrammes PNG écrits par `jeu spectre` (visualisation locale, régénérables).
/voix_spectres/
